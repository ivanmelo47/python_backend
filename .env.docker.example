# Example environment file for docker-compose
# Copy to `.env.docker` and edit values before running `docker-compose up`

# Database settings (point to your host DB)
DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=python_backend_db

# App settings
PYTHONUNBUFFERED=1
